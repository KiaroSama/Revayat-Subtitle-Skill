# Issue tracker

This local repository has no configured remote. Development tasks use Markdown
under `.scratch/`. The subtitle workflow and its file contracts live in the
shipped skill, not in the development tracker.
